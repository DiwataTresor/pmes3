/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: [
            "images.pexels.com",
            "lh3.googleusercontent.com",
            "indexrdc.com",
            "pmes.vercell.app",
            "localhost"
        ]
    }
}

module.exports = nextConfig
